package org.proleesh.repository;

import org.proleesh.entity.MV;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 * @author sung-hyuklee
 */
public interface MVRepository extends JpaRepository<MV, Long> {
}
